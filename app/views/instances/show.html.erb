<div class="container-instance-index">
  <div class="show-card">
    <p>You are currently looking at the <%= @instance.name %> exchange group.

    <!-- directs the user to create information if not yet made for the instance, else edit their information -->
    <% if @beer %>
      You have registered your preferences and shipping information for this exchange, and they are as follows: </p>
      <div class="show-content">
        <p> name: <%= @beer.name %> </p>
        <p>address: <%= @beer.address %> </p>
        <% if @beer.address_line2.present? %>
          <p>address line 2: <%= @beer.address_line2 %> </p>
        <% end %>
        <p> phone number: <%= @beer.phone %> </p>
        <p> preferences: <%= @beer.preferences %> </p>
        <p> email: <%= @beer.email %> </p>
      </div>
      <p>
        Would you like to make changes? <%= link_to "Click here", instance_beer_path(@instance, @beer), class: "" %>
        to edit your information.
      </p>
    </div>
      <% else %>
        You have not registered your preferences and shipping information yet!
        <%= link_to "Click here", new_instance_beer_path(@instance), class: "" %>
        to get started so your secret santa can send you your gifts.
      </p>
    </div>
  <% end %>

  <div class="send-mail">
    <% if @instance.user == current_user %>
    <p> If you are sure you are ready to send out the emails to everyone in your exchange group click here.
      Please make sure everyone is ready before clicking
    </p>
      <%= link_to "Send the e-mails", instance_mailer_path(@instance), :class  => "homepage-get-started" %>
    <% end %>
  </div>
</div>
